__pycache__/
*.pyc
node_modules/
src-tauri/target/
dist/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Terminal session dumps (iTerm2 AutoLog writes these into the cwd of
# whichever shell is logging; the grove does not write them). Names vary
# -- Log at <ts>, NEWLog, LATESTLog, WHAT!Log, NEW-SEPxxLog -- so anchor
# on the shared 'Log at <timestamp>.txt' tail.
*Log at *.txt
LATESTLog*.txt

# macOS
.DS_Store

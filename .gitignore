__pycache__/
*.pyc
node_modules/
src-tauri/target/
dist/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Session log dumps (the spruce-grove CLI writes these to cwd).
# Dump names vary -- Log at <ts>, NEWLog, LATESTLog, WHAT!Log,
# NEW-SEPxxLog -- so anchor on the shared 'Log at <timestamp>.txt' tail.
*Log at *.txt
LATESTLog*.txt

# macOS
.DS_Store

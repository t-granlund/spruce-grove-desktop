{"default":{"identifier":"default","description":"Core APIs for the main window. Without core:event the webview's listen() calls are permission-denied and every streamed update (chunks, thoughts, tools, turn-end, CLI logs) silently never arrives — the app looks 'stuck streaming' forever. This file is the fix for exactly that.","local":true,"windows":["main"],"permissions":["core:default"]}}